%!TEX TS-program = xelatex
%!TEX encoding = UTF-8 Unicode
\documentclass[$fontsize$, a4paper]{article}

% LAYOUT
%--------------------------------
% Margins
\usepackage{geometry}
\geometry{$geometry$}

% Do not indent paragraphs
\setlength\parindent{0in}

% Enable multicolumns
\usepackage{multicol}
\setlength{\columnsep}{1cm}
%\setlength{\columnseprule}{1pt}
%\def\columnseprulecolor{\color{black}}

% Uncomment to suppress page numbers
\pagenumbering{gobble}

% LANGUAGE
%--------------------------------
% Set the main language
$if(lang)$
\usepackage{polyglossia}
\setmainlanguage{$lang$}
$endif$

% TYPOGRAPHY
%--------------------------------
\usepackage{fontspec}
\usepackage{xunicode}
\usepackage{xltxtra}
\usepackage{fontawesome}
% converts LaTeX specials (quotes, dashes etc.) to Unicode
\defaultfontfeatures{Mapping=tex-text}
\setromanfont [Ligatures={Common}, Numbers={OldStyle}]{$mainfont$}
% Cool ampersand
\newcommand{\amper}{{\fontspec[Scale=.95]{$mainfont$}\selectfont\itshape\&}}

\newfontfamily{\FA}{FontAwesome}

% MARGIN NOTES
%--------------------------------
\usepackage{marginnote}
\newcommand{\note}[1]{\marginnote{\scriptsize #1}}
\renewcommand*{\raggedleftmarginnote}{}
\setlength{\marginparsep}{7pt}
\reversemarginpar

% HEADINGS
%--------------------------------
\usepackage{sectsty}
\usepackage[normalem]{ulem}
\sectionfont{\rmfamily\mdseries}
\subsectionfont{\rmfamily\mdseries\scshape\normalsize}
\subsubsectionfont{\rmfamily\bfseries\upshape\normalsize}

% PDF SETUP
%--------------------------------
\usepackage{hyperref}
\hypersetup
{
	pdfauthor={$name$},
  pdfsubject={$name$'s $filetype$},
  pdftitle={$name$'s $filetype$},
  colorlinks, breaklinks, xetex, bookmarks,
  filecolor=black,
  urlcolor=[rgb]{0.117,0.682,0.858},
  linkcolor=[rgb]{0.117,0.682,0.858},
  linkcolor=[rgb]{0.117,0.682,0.858},
  citecolor=[rgb]{0.117,0.682,0.858}
}

% DOCUMENT
%--------------------------------
\begin{document}
\begin{center}

	% NAME
	%-------------------------------
	{\Huge $name$}$if(suffix)$,  {\large $suffix$}\\[.2cm]$endif$

	\vspace{2pt}
	\begin{multicols}{3}
		% CONTACT
		%------------------------------
		$phone$
		\columnbreak

		$if(blog)$
		\href{http://$blog$}{{\FA \faRss}$blog$}
		$endif$
		\vspace{2pt}

		$if(github)$
		\href{http://github.com/$github$}{{\FA \faGithub}$github$}
		$endif$
		\vspace{2pt}

		$if(emailprefix)$
		$if(emailsuffix)$
		\href{mailto:$emailprefix$@$emailsuffix$}{{\FA \faEnvelope}$emailprefix$@$emailsuffix$}
		$endif$
		$endif$

		\columnbreak

		$for(address)$
		$address$\\
		$endfor$

	\end{multicols}

	% QUOTE
	%------------------------------
	\vspace{1pt}
	{\it `$quote$`}
\end{center}

% INTRO
%------------------------------
$if(intro)$
$intro$
$endif$

% PROJECTS & RESEARCH
%------------------------------

$if(projects)$
\section*{Projects \& Contributions}
\vspace{-20pt}
\rule{15cm}{0.04cm}%
\noindent
$for(projects)$

\note{$projects.language$}

\underline {\textsc {$projects.project$} } : $if(projects.url)$ {\small \it {\href{http://$projects.url$}{$projects.url$}} $endif$

\begin{itemize}
		$for(projects.desc)$
	\item $projects.desc$
		$endfor$
\end{itemize}
$endfor$
$endif$
% \vfill

$if(publications)$
\section*{Publications}
\vspace{-15pt}
\rule{15cm}{0.04cm}%
\noindent
$for(publications)$

\note{$publications.year$}

\underline{\textsc {$publications.project$} }: $if(publications.url)$ {\small \it {\href{http://$publications.url$}{$publications.url$}} $endif$

\begin{itemize}
		$for(publications.desc)$
	\item $publications.desc$
		$endfor$
\end{itemize}
$endfor$
$endif$

% WORK EXPERIENCE
%-----------------------------
$if(experience)$
\section*{Work Experience}
\vspace{-17pt}
\rule{15cm}{0.04cm}%
\noindent
$for(experience)$

	\note{$experience.years$}
	% \emph{$experience.job$}\\
	\underline{\textsc {$experience.job$}}: {\small \it{$experience.employer$} ($experience.city$)}
	\begin{itemize}
			$for(experience.desc)$
		\item $experience.desc$
			$endfor$
	\end{itemize}
$endfor$
$endif$


% \vfill
\vspace{2pt}

\begin{multicols}{2}

	% EDUCATION
	%----------------------------
	$if(education)$
	\section*{Education}
\vspace{-15pt}
\rule{15cm}{0.04cm}%
	$for(education)$
	\noindent
	\textbf {$if(education.degree)$ $education.degree$$endif$, $if(education.marks)$ $education.marks$} - {\scriptsize $education.year$} \\
	\emph{$education.institute$}$if(education.city)$, $education.city$$endif$ $endif$\\[.1cm]
	$endfor$
	$endif$

	%	\vfill\null
	%	\columnbreak

	% ACHIEVEMENTS
	%-----------------------------

	$if(achievements)$
	\section*{Achievements}
	\noindent
	$for(achievements)$
	\textbf{$achievements.award$} - {\scriptsize $achievements.years$}\\
	\emph{$achievements.desc$}\\\\
	$endfor$
	\columnbreak
	$endif$

	% REFERENCES
	%----------------------------

	$if(references)$
	\section*{References}
	\noindent
	$for(references)$
	\emph{$references.name$}\\
	{$references.organization$}\\
	{$references.designation$}\\
	{$references.contact$}\\
	$endfor$
	$endif$



\end{multicols}


\end{document}
